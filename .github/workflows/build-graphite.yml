name: Build Graphite

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build
        uses: yuchenxue123/builder/.github/actions/build-repository-java
        with:
          working-path: ./SonaGG/Graphite
          repository: SonaGG/Graphite
          branch: 1.8.9
          java-version: 21
